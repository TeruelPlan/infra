image:
  repository: ghcr.io/teruelplan/plantogether-trip-service
  tag: latest

service:
  port: 8081

grpc:
  enabled: true
  port: 9081

ingress:
  path: /api/v1/trips

env:
  - name: SPRING_PROFILES_ACTIVE
    value: prod
  - name: TRIP_SERVICE_GRPC_PORT
    value: "9081"
