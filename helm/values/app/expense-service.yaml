image:
  repository: ghcr.io/teruelplan/plantogether-expense-service
  tag: latest

service:
  port: 8084

grpc:
  enabled: true
  port: 9084

ingress:
  path: /api/v1/trips

env:
  - name: SPRING_PROFILES_ACTIVE
    value: prod
